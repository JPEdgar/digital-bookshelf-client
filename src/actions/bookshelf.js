import * as api from "../api";

/*
const getBooks = async (options) => {
  try {
    // const { data } = await api.getBooks(options);
    // return data;
  } catch (error) {
    console.log(error);
  }
};

const getBook = async (id, options) => {
  try {
    // const { data } = await api.getBook(id, options);
    // return data;
  } catch (error) {
    console.log(error);
  }
};

const updateBook = async (update, options) => {
  try {
    // const { data } = await api.updateBook(update, options);
    // return data;
  } catch (error) {
    console.log(error);
  }
};

const setBook = async (newBook, options) => {
  try {
    // const { data } = await api.setBook(newBook, options);
    // return data;
  } catch (error) {
    console.log(error);
  }
};

const deleteBook = async (id, options) => {
  try {
    // const { data } = await api.deleteBook(id, options);
    // return data;
  } catch (error) {
    console.log(error);
  }
};

const toggleFavorite = async (book) => {
  //
};

export { getBooks, getBook, updateBook, setBook, deleteBook, toggleFavorite };
*/