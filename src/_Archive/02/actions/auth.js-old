import * as api from "../api";

const getUser = async (email) => {
  try {
    const data = await api.getUser(email);
    return data;
  } catch (error) {
    console.log(error);
  }
};

export { getUser };
